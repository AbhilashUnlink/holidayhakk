import { windowHeight, windowWidth } from "../constants/ScreenSize";

const aspectRatio = {
  //home trending tab
  trendingPostWidth: windowWidth * 0.48,
  trendingTabHeight: windowHeight * 0.35,
  //login
  loginLogoWidth: windowWidth * 0.6,
  loginLogoHeight: windowHeight * 0.2,
  loginInputFieldWidth: windowWidth * 0.85,

  //
  fullHeight: windowHeight,
};
export default aspectRatio;
